# Roadmap

迭代计划与想法 backlog。优先级：P0 最高。完成后移入 CHANGELOG。

## P0 - 下一轮迭代

- [ ] 停止生成按钮（流式输出中途取消）
- [ ] 删除历史对话（对话列表加删除入口，需确认机制）
- [ ] 对话导出为 Markdown 文件
- [ ] gemma3-4b 下载并验证多模态图片对话全链路
- [ ] 模型性能对比视图：汇总各对话 metrics，按模型分组展示 TTFT/tok/s 均值

## P1 - 体验优化

- [ ] UI 服务进程守护（崩溃自动重启，systemd user unit 或 supervisor）
- [ ] 搜索后端可切换：DuckDuckGo / 自建 SearXNG（结果质量更好，需额外容器）
- [ ] 对话内 token 用量显示（prompt tokens、上下文剩余估算）
- [ ] 联网搜索支持追问时基于上下文改写搜索 query（当前直接用最后一句）
- [ ] 暗色/亮色主题切换

## P2 - 模型与部署

- [ ] gemma3-27b：等社区可靠量化版（AWQ/GPTQ），评估 24G 显存预算
- [ ] `--enforce-eager` 可选模式：冷启动更快，推理慢 20-30%，适合调试
- [ ] 评估 Qwen3 新版本/新量化（FP8 on Ada）
- [ ] vLLM 镜像版本固定（当前 latest，升级需回归测试）

## 已知问题

- DuckDuckGo 中文搜索结果质量一般，时效性查询（天气、股价）基本无效
- 图片附件 base64 直接进上下文，大图/多图会快速消耗 max-model-len
- 单模型约束下，对话中切换模型需要先停再启，体验割裂（受显存限制，无解，只能优化引导）

## 想法（未评估）

- RAG：本地文档向量化检索（需引入 embedding 模型，显存不够，可能要 CPU 跑）
- Agent 模式：工具调用循环（搜索 → 阅读网页 → 再答），小模型稳定性待验证
- OpenAI TTS/whisper 语音输入输出
